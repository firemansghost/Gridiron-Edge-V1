# Gridiron Edge Environment Variables
# Copy this file to a local env file and fill in secrets; never commit real secrets.

# =============================================================================
# Core Configuration
# =============================================================================

# Application environment
APP_ENV=development               # development | preview | production

# Timezone for all date/time operations
TZ=America/Chicago                # lock UI and jobs to CST/CDT

# Model version for as-of state tracking
MODEL_VERSION=v0.0.1              # bump when recalibrating models

# Data sources configuration path
DATASOURCES_CONFIG_PATH=datasources.yml

# =============================================================================
# Postgres / Supabase (server runtimes)
# =============================================================================

# Use the POOLED Postgres connection string for Vercel/serverless (transaction mode).
DATABASE_URL=postgres://YOUR_POOLED_CONNECTION_STRING

# Shadow DB URL for migrations (keep same cluster, different DB or schema).
SHADOW_DATABASE_URL=postgres://YOUR_SHADOW_DB_CONNECTION_STRING

# =============================================================================
# Supabase project metadata (non-secret)
# =============================================================================

# Supabase project URL
SUPABASE_URL=https://tccqmxcaledmlkybjqef.supabase.co

# =============================================================================
# Supabase keys (leave blank here; NEVER commit real values)
# =============================================================================

# Public browser key (only if we ever use supabase-js in the browser; RLS enforced)
SUPABASE_ANON_KEY=

# Service role key (SERVER USE ONLY: ETL/jobs; NEVER in the client)
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# External API Keys (Reserved for future providers; blank for now)
# =============================================================================

# Sports betting data
ODDS_API_KEY=

# Weather data for game conditions
WEATHER_API_KEY=

# Recruiting data
RECRUITING_API_KEY=
